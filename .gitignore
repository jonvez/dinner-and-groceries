node_modules/
.next/
out/
dist/
build/
next-env.d.ts
*.tsbuildinfo
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/
# Seeded E2E session storageState (live local session tokens; re-seeded per run).
e2e/.auth/
.playwright-mcp/
.env*
!.env.example
.DS_Store
.vercel
# Editor + local orchestration scratch (SDD progress ledger, task briefs).
.idea/
.superpowers/
# Agent worktrees — whole checkouts of other branches, created by subagent
# dispatch. Untracked here they show up in `git status` on main, where a
# `git add -A` would commit an entire nested checkout.
.claude/worktrees/
