# Example environment — copy to `.env.local` and fill in. NEVER commit real values.
# Real values live in GCP Secret Manager (prod, bound to Cloud Run) and in each
# developer's local .env.local (gitignored).

# Supabase (populated by #2 once supabase/ lands; `supabase status` prints these
# for the local stack).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
